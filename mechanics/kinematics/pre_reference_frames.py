def explain_reference_frames():
    print("A reference frame is a perspective from which motion is observed and measured.\n")
    print("Example: A passenger on a train sees a ball at rest, but an observer outside sees it moving with the train.")

if __name__ == "__main__":
    explain_reference_frames()
